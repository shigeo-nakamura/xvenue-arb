# Generated by Cargo
# will have compiled files and executables
debug/
target/
aws-kms.txt
encrypted-private-key.txt
scripts/myenv/
run_marketmake.sh
scripts/venv/
*.env
!scripts/debot.env
*.log
*.md
!online_adaptation_plan.md
# docs/ holds private strategy notes — keep locally, don't publish.
# This repo is public, so the design doc (DESIGN.md) and Phase 0
# methodology (phase0.md) live on the operator's machine only.
docs/
*.patch
pairtrade_history.json
*.swp
.venv/
*.jsonl
*.bin
.claude/
